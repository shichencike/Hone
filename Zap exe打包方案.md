# Zap 功能提案：zap build --exe 将脚本打包为独立可执行文件

- 状态：已实现（2026-08-05 落地，见第八节）
- 关联代码：`src/bundle.rs`（打包格式 + 自释放启动器）、`src/main.rs`（`build --exe` 命令与入口检测）
- 兼容性：向下兼容，仅新增命令；普通 zap 启动时只读自身尾部 56 字节判断是否打包，开销可忽略

---

## 一、目标

为 Zap 增加一个构建命令，将 .zp 脚本与 Zap 解释器打包成一个独立的可执行文件（Windows PE / Linux ELF），使用户能够：

- 双击运行，无需安装 Zap
- 单文件交付，方便分发
- 保持 Zap 脚本的全部功能，包括 load 动态库、import 远程模块、db 持久化等
- 保持 Zap 一贯的轻量特性（输出文件体积 ≈ 解释器 + 脚本大小）

## 二、设计原则

1. **不依赖外部 C 编译器**：不引入对 GCC/Clang/MSVC 的依赖。
2. **不修改解释器核心**：复用现有 zap 可执行文件作为运行时，只做打包封装。
3. **体积优先**：输出文件大小接近解释器 + 脚本的原始大小，无额外膨胀。
4. **行为一致**：打包后的 exe 执行结果与 `zap run script.zp` 完全一致。
5. **用户无感**：运行时自动释放缓存，不干扰用户工作目录；执行完毕后自动清理（`--keep-cache` 可保留以加速下次启动）。
6. **版本信息明确**：生成的 exe 内嵌 Zap 版本、构建时间、脚本名，可用 `--version` 查看。

## 三、实现方案

采用 **解释器 + 脚本打包，运行时就地释放** 的方式：

**文件布局（`src/bundle.rs`）：**

```
[0 .. zap_len)                    zap.exe 原始字节（运行时）
[zap_len .. +name_len)            脚本文件名（UTF-8）
[... +script_len)                 脚本内容
[文件末尾 56 字节]                定长尾部块（小端）
```

尾部块字段：

| 偏移 | 长度 | 字段 |
|---|---|---|
| 0 | 8 | magic `ZAPBND01` |
| 8 | 8 | zap_len（运行时字节数） |
| 16 | 4 | name_len（脚本名字节数） |
| 20 | 4 | script_len（脚本字节数） |
| 24 | 2+2+2+2 | ver_major / ver_minor / ver_patch / flags |
| 32 | 8 | 构建时间戳（UNIX 秒） |
| 40 | 8 | zap 部分 FNV-1a 校验和 |
| 48 | 8 | 脚本部分 FNV-1a 校验和 |

**构建阶段**：读取 script.zp → 以当前 zap 可执行文件为运行时 → 组装输出单文件。

**运行阶段（自释放启动器）**：

1. 启动时读取自身尾部 56 字节，校验 magic 与双段校验和（损坏时报错提示重建）；
2. 释放内嵌 zap.exe 与 script.zp 到 `.zap_cache` 隐藏目录（已存在且校验一致则跳过，避免重复写盘）；
3. 当前目录不可写时回退系统临时目录（`zap-bundle-<脚本名>`）；
4. 子进程调用 `.zap_cache/zap.exe script.zp [参数...]`，继承标准流，传播退出码；
5. 默认清理缓存；`--keep-cache` 保留以加速下次启动。

**路径处理**：脚本的工作目录 = exe 运行时的当前目录，load 相对路径、read_file 等行为与 `zap run` 一致。

**版本信息**：内嵌 Zap 版本号（ver_major/minor/patch）、构建时间戳、源脚本名、双段校验和。

## 四、命令行接口

```
zap build --exe <script.zp> [-o <输出文件名>] [--icon <图标文件>] [--version]
```

- `-o <out>`：指定输出文件名（默认 `<脚本名>.exe`）
- `--icon <ico>`：当前版本接受参数但输出警告并忽略（Windows PE 图标资源注入暂未实现，见第七节）
- `--version`：显示 `zap build --exe` 版本信息，不执行构建

打包后运行：

```
./my_app                     # 释放并执行脚本
./my_app --version           # > my_app (Zap v0.1.0) built at 2026-08-05 14:30:00 UTC
                             # > script: script.zp
./my_app --keep-cache ...    # 执行后保留 .zap_cache 缓存
./my_app <其他参数>          # 原样传递给脚本（args.get / args.has）
```

## 五、优势与边界

**优势**

| 优势 | 说明 |
|---|---|
| 零失败率 | 不涉及代码翻译，只是打包，不存在无法转换的情况 |
| 体积极小 | 输出文件 ≈ zap.exe + 脚本大小（release 构建通常 < 2.1MB） |
| 依赖不变 | 运行时仍依赖操作系统 API 和系统 DLL，不引入新依赖 |
| 行为一致 | 完全等同于 zap run，所有内置函数和 load 行为一致 |
| 可迁移性 | 生成的 exe 可随意复制到其他目录甚至 U 盘，运行时自动适配 |
| 版本可溯 | 内嵌版本信息与双段校验和，方便识别、升级与完整性校验 |

**边界**

| 边界 | 说明 |
|---|---|
| 需要当前目录可写 | 运行时需创建 .zap_cache；不可写时自动回退系统临时目录 |
| 不支持直接生成机器码 | 仍是解释执行，速度与 zap run 相同 |
| 多实例并发 | 释放前按校验和检查，已存在且一致则不重复写盘；执行后默认清理 |

## 六、与现有工具链的关系

- `zap run`：直接解释执行，适合开发调试
- `zap build --dll`：将脚本编译为动态库，供其他语言调用（需 C 编译器）
- `zap build --exe`：将脚本打包为独立可执行文件，面向最终用户分发

三者互补，覆盖开发集成交付全流程。

## 七、未来扩展方向（暂不实现）

- `--icon`：Windows PE 图标资源注入（需引入资源编辑器或自写 .rsrc 段）
- 支持压缩 / 加密脚本，防止逆向
- 支持指定释放路径（如系统临时目录）
- 支持嵌入外部资源（如 DLL、配置文件）

## 八、实现说明

- 校验和：FNV-1a 64 位（零依赖，`src/bundle.rs`）
- 时间戳格式化：复用 `builtins::format_timestamp`（UTC）
- 缓存目录隐藏属性：Windows 下 `SetFileAttributesW(FILE_ATTRIBUTE_HIDDEN)`（`winapi` 的 `fileapi`/`winnt`）
- 体积提示：嵌入的是构建时正在运行的 zap 本体；`cargo build --release` 后打包体积最小
- 验证：`examples/` 下可用任意脚本打包测试（如 `zap build --exe examples/hello.zp`）
