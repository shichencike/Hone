# Hone 错误处理与自动恢复机制增强方案

- 状态：已实现（2026-08-05 全部落地）
- 实现范围：3.1 try-catch / 3.2 错误码细化 + `--explain` / 3.3 `--restart` 名刀机制 / 3.4 `--resume` 检查点恢复
- 关联代码：`src/error.rs`、`src/main.rs`、`src/interp.rs`、`src/parser.rs`、`src/checker.rs`、`src/ast.rs`、`src/builtins.rs`、`src/lexer.rs`、`src/sysmod.rs`
- 示例脚本：`examples/try_catch.hn`、`examples/restart_demo.hn`、`examples/resume_demo.hn`
- 兼容性：向下兼容，全部为可选新增特性，不影响现有脚本

---

## 零、实现说明（实际用法速查）

```
# 错误码解释
hone explain H201

# 自动重启（失败重拉，默认最多 3 次，间隔 1s/3s/10s 递增）
hone run --restart[=N] --backoff=a,b,c --restart-on=H200,H401 script.hn

# 检查点恢复（db.set 自动落盘 ~/.hn/state/，脚本变更后检查点自动失效）
hone run --resume script.hn

# 错误捕获与主动抛出（语法层）
try { resp = http_get("http://x/data"); }
catch e { print(e.code + ": " + e.message); resp = db.get("cached"); }
throw "user error";            # 抛 H600
throw e;                       # 重抛已捕获的 error 值
```

错误对象字段：`e.code`、`e.message`（str），`e.file`、`e.context`（str），`e.line`、`e.col`（int）。
注意 Hone 为块作用域：try/catch 块内赋值的变量在块外不可见，跨块共享的变量需在块前声明。

---

## 一、背景

Hone 目前的错误处理设计是报错即停止：对于脚本内部错误（如类型错误、未定义变量等）会给出精准的 `error[Hxxx]` 并终止执行。这保证了确定性，也减少了调试难度。但在实际生产辅助场景中（尤其是作为瑞士军刀长期驻留运行），仍存在以下可改进空间：

1. 网络抖动、临时文件锁、DLL 偶发崩溃等可恢复错误会导致进程直接退出，需要人工介入。
2. 脚本本身无法捕获错误并执行回退逻辑（如使用缓存、重试、降级）。
3. 进程崩溃后缺乏自动拉起的机制，在无人值守的服务器上可能导致任务中断。

---

## 二、现状对照（代码调研结论）

### 2.1 错误码体系

`src/error.rs` 的 `codes` 模块目前定义了 16 个错误码，分区尚不系统：

| 错误码 | 含义 | 备注 |
|---|---|---|
| H001 | 类型冲突 | 静态/运行期 |
| H002 | 未定义的变量或函数 | 静态/运行期 |
| H003 | 无法自动推导类型 | 静态 |
| H004 | 运算符重载歧义 | 静态 |
| H005 | 语法错误 | 词法/语法 |
| H006 / H007 | 字符串转整数 / 浮点数失败 | 运行期 |
| H008 | 条件表达式必须是 bool | 静态/运行期 |
| H009 / H010 | 除零 / 整数溢出 | 运行期 |
| H011 / H012 | 参数数量不匹配 / 递归过深 | 运行期 |
| H200 | 网络请求失败 | 网络 |
| H300 | 系统调用失败 | 系统/DLL |
| H404 | 文件或库不存在 | 文件 |
| H999 | 尚未实现 | 内部 |

可见「网络 / 文件 / DLL」虽有雏形（H200/H300/H404），但粒度粗：网络错误不区分超时与连接拒绝，文件错误不区分不存在与权限不足，DLL 错误不区分加载失败与参数校验失败。

### 2.2 错误传递机制

- 静态检查：`checker::Checker::check(&program, ...) -> Result<(), ZError>`。
- 运行期：`interp.rs` 中所有 `exec_*` / `eval_*` 均返回 `Result<Flow, ZError>`；`runtime_err()` 统一构造带 file / line / col / len 的错误。
- 展示：`ZError` 的 `Display` 输出 `error[Hxxx]: msg` + `--> 文件:行:列` + 代码片段 + caret 标记 + `help:` 建议。

结论：错误已携带「错误码 + 行号 + 列号 + 上下文（代码行）+ 修复建议」，与方案 1 中「错误对象应包含错误码、行号、上下文信息」的要求天然吻合，只需在语法层暴露给脚本即可。

### 2.3 CLI 与退出码

- `main()` 目前仅区分 `ExitCode::SUCCESS`（0）与 `ExitCode::FAILURE`（1），任何 `Err(ZError)` 都打印后退出码 1。
- 无子进程监控、无重启逻辑、无断点/恢复机制。
- 无 `try` / `catch` 关键字（`parser.rs` 无相关词法）。

---

## 三、建议新增功能

### 3.1 错误捕获与处理（语法层）

参考其他脚本语言，增加 try-catch 结构，允许开发者显式处理可预见的错误：

```
try {
    resp = http_get("https://api.example.com/data");
} catch e {
    log.error("请求失败：" + e.message);
    resp = db.get("cached_data");
}
```

**设计要点：**

- **错误对象字段**：`code`（错误码，如 "H200"）、`message`（描述）、`file`、`line`、`col`、`context`（出错代码行文本）。与现有 `ZError` 字段一一对应，实现成本低。
- **类型方案**：引入内置类型 `error`（或 `Error`），`catch e` 中的 `e` 由编译器自动推导为 `error` 类型；提供 `e.code`、`e.message` 等字段访问。为保持静态强类型风格，建议 catch 变量类型固定为 `error`，不做多态。
- **不可恢复错误**：内存越界等 Rust 层 panic（非 `ZError`）不进 try-catch，仍保持原有崩溃行为；try-catch 只接管通过 `ZError` 通道返回的脚本级错误，天然满足「明确区分风险区域」。
- **与现有管线的对接点**：
  - `ast.rs`：新增 `Stmt::Try { body, catch_var, handler }` 节点。
  - `parser.rs`：解析 `try { ... } catch e { ... }`（`catch` 目前仅作为歧义检测词出现，需加入保留字表）。
  - `checker.rs`：检查 handler 中 `e` 的类型（`error`）及字段访问合法性。
  - `interp.rs`：`exec_stmt` 中执行 body 时捕获 `Err(ZError)`，将 `ZError` 转换为 `Value::Error`（新增值变体），绑定到 `catch_var` 后执行 handler；`Flow::Normal` 继续。
  - 注意：`throw`（主动抛错）可作为配套语法一并引入，或第一期只做捕获不做抛出。

### 3.2 错误类型细化（报错信息完善）

建议将 H001~H999 按功能区段系统化细分，并对现有码做向后兼容的归位：

| 区段 | 分类 | 建议码位 |
|---|---|---|
| H001 ~ H099 | 语法/类型/推导（保留现状） | 不动 |
| H100 ~ H199 | 词法与语法细分 | H101 非法字符、H102 未闭合字符串、H103 未闭合注释、H104 缺少分号等 |
| H200 ~ H299 | 网络错误 | H200 网络请求失败（保留）、H201 连接超时、H202 连接拒绝、H203 DNS 解析失败、H204 非 2xx 响应 |
| H300 ~ H399 | 系统/DLL 错误 | H300 系统调用失败（保留）、H301 DLL 加载失败、H302 DLL 参数校验失败、H303 权限不足 |
| H400 ~ H499 | 文件错误 | H404 文件或库不存在（保留）、H401 文件不存在、H402 权限不足、H403 文件被占用/锁定 |
| H500 ~ H599 | 运行期逻辑错误 | 除零、溢出、递归深度等（从 H009 等归位或增补） |
| H900 ~ H999 | 内部/未实现 | H999 尚未实现（保留） |

**配套增强：**

- 每个错误码在 `help:` 之外补充文档锚点，例如 `help: 参见 docs#H201 连接超时`，可在 `error.rs` 中维护一份 `code -> 文档链接/解释` 的静态映射表（纯数据，体积可控）。
- `--explain H201` 子命令：直接打印某错误码的完整解释与示例，方便 CLI 场景自查。

### 3.3 崩溃自动重启（名刀机制）

增加 `--restart` 开关，让 Hone 在脚本因非零退出码（崩溃）终止时自动重新拉起。

**CLI 设计（`hone run` 内置）：**

```
hone run --restart[=N] script.hn
# 示例
hone run --restart=3 --backoff=1,3,10 script.hn
```

- `--restart=N`：最大重启次数，默认 3；N=0 或省略即不重启（保持现状）。
- `--backoff=a,b,c`：重启间隔递增序列（默认 `1,3,10` 秒），避免频繁重启消耗资源。
- 语义：脚本**正常结束（退出码 0）不重启**；`ZError` 终止（退出码 1）按策略重启；Rust panic / 信号终止视为崩溃，同样纳入重启计数。
- 防死循环：重启次数达到上限后打印汇总日志（各次错误码统计）并以失败退出；可支持 `--restart-on=H200,H404` 错误码白名单，只对可恢复错误重启，`H001` 这类确定性问题直接退出。

**外部包装示例（不内置时的兜底）：**

```
// monitor.hn 作为外部包装
while true {
    exit_code = sys.run("hone run script.hn");
    if exit_code == 0 { break; }
    restart_count++;
    if restart_count > 3 { break; }
    time.sleep(3000);
}
```

**实现落点：** `main.rs` 的 `run_cli` 在 `run` 分支解析 `--restart` 参数后，将「单次运行」抽为可重复调用的函数，外层循环按退出码/错误码决定是否重拉；`run_file` 目前返回 `Result<(), ZError>`，可进一步返回结构化结果（错误码 + 退出码）供监控层判断。内置实现比 `monitor.hn` 包装更优：无需额外进程、可精确区分「正常结束 / 可恢复错误 / 确定性错误」。

### 3.4 状态持久化配合重启

结合内置 db 模块，允许脚本在关键节点写入检查点。重启后通过 `--resume` 参数或自动读取恢复上次进度，避免重复工作。

```
hone run --resume script.hn
```

**设计要点：**

- 检查点写入：脚本在关键节点调用 `db.set("checkpoint", {...})`（数据落盘到 `~/.hn/state/` 或 db 文件）。
- `--resume` 语义：启动时自动加载上一份检查点注入环境（如 `sys.get_checkpoint()`），脚本据此跳过已完成阶段；配合 3.3 的重启，实现「崩溃 → 重启 → 从断点继续」的闭环。
- 版本标记：检查点携带脚本哈希/版本号，脚本变更后自动失效，避免脏恢复。
- 第一期可只做「手动 `--resume` + db 读取」，第二期再做「崩溃时自动 dump 调用栈/局部状态」。

---

## 四、价值与收益

- **降低运维成本**：减少因偶发错误导致的手动介入次数。
- **提升可靠性**：无人值守脚本可长期稳定运行。
- **保持轻量**：以上功能体积增加可控制在数百 KB 内，不影响核心定位。
- **向下兼容**：不影响已有脚本，仅新增可选特性。

---

## 五、可能的实现路径

| 阶段 | 内容 | 状态 |
|---|---|---|
| 短期 | `hone run` 增加 `--restart[=N]` 与 `--backoff` 开关，子进程/内层循环监控自身；错误码分区表 + `--explain` | ✅ 已实现 |
| 中期 | 引入 `try { } catch e { }` 语法（含 `error` 类型、`Value::Error` 变体）；错误类型细化落地 | ✅ 已实现 |
| 长期 | `--resume` 与检查点自动恢复机制（db 检查点 + 版本标记） | ✅ 已实现 |

**落地顺序**：先 3.3（`--restart`，改动面最小），再 3.1（try-catch，语法层工作量最大），最后 3.4（resume）。

---

## 六、风险与兼容性说明

1. **try-catch 与静态类型**：`error` 类型需纳入类型系统与检查器；`catch e` 中 `e` 固定为 `error` 类型可避免泛型复杂度。
2. **重启死循环**：`--restart` 必须配错误码白名单或次数上限，避免确定性错误（如语法错误）无限重启。
3. **panic 与 ZError 的边界**：Rust panic 默认不触发 try-catch（保持崩溃行为），只有 `ZError` 通道的错误可捕获；文档需明确此边界。
4. **保留字冲突**：`try` / `catch` 进入保留字表后，现有脚本若将其用作标识符需检查兼容性（当前代码中 `catch` 仅作为歧义检测词，无实际使用，风险低）。
5. **体积控制**：错误码解释表与 `--explain` 为纯静态数据，对二进制体积影响可忽略。
